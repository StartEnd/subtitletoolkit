# Subtitle Toolkit GSC Weekly Tracker

Use this file once per week after the SEO deployment is live. Keep entries short and evidence-based.

## How To Export

1. Open Google Search Console -> Performance -> Search results.
2. Set the date range to the last 28 days.
3. Export Queries and Pages.
4. In Plausible or your analytics source, record organic pageviews and tool events for the same 28-day window.
5. Save the CSV files under local `gsc-exports/`; this directory is intentionally ignored by git.
6. Check the local review inputs and print the exact analyzer command:

```bash
pnpm gsc:review:ready -- --organic-pageviews 123 --tool-starts 12 --tool-outputs 3 --week-of 2026-06-08
```

7. Build the current site, then run the local analyzer with the two CSV files:

```bash
pnpm build
pnpm gsc:analyze -- --queries gsc-exports/queries.csv --pages gsc-exports/pages.csv --promotion-log PROMOTION_LOG.md --organic-pageviews 123 --tool-starts 12 --tool-outputs 3
```

Replace `123`, `12`, and `3` with same-window Plausible numbers.
Tool starts = `subtitle_tool_edit_input` + `subtitle_tool_adjust_setting` + `subtitle_tool_upload_file` + `subtitle_tool_load_sample`.
Tool outputs = `subtitle_tool_copy_output` + `subtitle_tool_download_output`.
The analyzer normalizes trailing-slash differences between GSC exports and the built Astro URLs.

8. Review the `Promotion Evidence Window` output so GSC/Plausible movement is compared against real external actions.
9. Fill one weekly row below from the `Weekly Summary Helper` output.
10. Fill the ad readiness row from the `Ad Readiness Gate` output.
11. Fill the traffic quality row from the `Traffic Quality Snapshot` output.
12. Pick one small batch for the next change; do not edit the same URL again until 7 to 14 days have passed.

## Weekly Summary

| Week of | Impressions | Clicks | CTR | Avg position | Pages with impressions | Pages with clicks | Main action shipped | Next review date |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- |
| 2026-06-01 | | | | | | | Initial SEO title, description, internal link, schema, and sitemap update deployed and production-verified. Submit Day 0 URLs in GSC. | 2026-06-08 |

## Opportunity Buckets

### Bucket A: Impressions >= 10, Clicks = 0

Use this bucket for CTR work: title, meta description, and search-result promise.

| Query | Page | Impressions | Position | Current title fit | Next change | Review after |
| --- | --- | ---: | ---: | --- | --- | --- |
| | | | | | | |

### Bucket B: Position 8 To 30

Use this bucket for ranking support: internal links, short exact-answer sections, and matching tool links.

| Query | Page | Impressions | Position | Missing support | Next change | Review after |
| --- | --- | ---: | ---: | --- | --- | --- |
| | | | | | | |

### Bucket C: Indexed, No Impressions

Use this bucket for crawl and intent checks: sitemap presence, hub links, tool links, and overly broad titles.

| Page | Indexed status | Sitemap? | Internal links? | Likely issue | Next change | Review after |
| --- | --- | --- | --- | --- | --- | --- |
| | | | | | | |

## Ad Readiness Snapshot

Only consider ad placement after the gate in `SEARCH_GROWTH_PLAYBOOK.md` is met.

| Week of | Organic pageviews last 28 days | Pages with impressions | Pages with clicks | Gate met? | Notes |
| --- | ---: | ---: | ---: | --- | --- |
| 2026-06-01 | | | | No | Waiting for same-window analytics pageviews and GSC clicks after Day 0 submission. |

Before changing `Gate met?` to Yes, `pnpm verify:seo:ready` must pass with ad placeholders below the tool workspace, hidden while inactive, and no `data-ads-enabled="true"` present before the gate.

## Traffic Quality Snapshot

Use this to avoid optimizing only for impressions. Low tool starts means the query/page promise may be wrong; low tool outputs means the tool UX or file workflow may need work before ads.

| Week of | Tool starts | Starts / organic pageviews | Tool outputs | Outputs / tool starts | Notes |
| --- | ---: | ---: | ---: | ---: | --- |
| 2026-06-01 | | | | | Waiting for same-window Plausible custom events after production deployment. |

## Change Log

Record only changes that can plausibly affect search behavior.

| Date | URLs changed | Change type | Reason | Verification |
| --- | --- | --- | --- | --- |
| 2026-06-01 | Priority guides, tools, hubs, sitemap | Titles, descriptions, internal links, schema, sitemap lastmod, SERP metadata audit | Improve zero-click impressions, query-title fit, guide-to-tool conversion, and crawl recency signals. | `pnpm verify:gsc:submit-ready` passed on production for commit `17ee9a3` |
| 2026-06-01 | Homepage and guides index | Internal links to YouTube upload failure, YouTube conversion, and SRT upload failure guides | Increase internal discovery and homepage/hub support for the current YouTube/SRT search-growth cluster. | `pnpm verify:gsc:submit-ready` passed on production for commit `e4b82f7` |
| 2026-06-01 | Guides index and platform caption failure guides | Internal links from `/guides/` plus GSC follow-up queue alignment | Increase discovery and inspection coverage for Video.js, JW Player, Plex, and Vimeo caption failure long-tail queries. | `pnpm verify:seo:ready` passed for commit `5c7012f` |
| 2026-06-01 | Homepage, guides index, upload-prep guides | Added upload-prep discovery links and SERP descriptions for ASS-to-SRT YouTube, subtitle cleanup, and HTML5 troubleshooting pages | Increase clicks from upload-prep and HTML5 subtitle troubleshooting searches while keeping the GSC queue tied to production evidence. | `pnpm verify:gsc:submit-ready` passed on production for commit `4653386` |
| 2026-06-01 | GSC Day 0 current queue | Added recently optimized sync repair, transcript, bilingual, overlap, and line-break guides to the current URL Inspection batch | Keep Search Console submission coverage aligned with the latest CTR-focused SERP copy and guide-index discovery work. | `pnpm gsc:day0:list -- --batch current` prints 60 URLs; `pnpm verify:gsc:day0` passed for 81 URLs plus sitemap index |
| 2026-06-01 | Plex naming, HTML tag cleanup, export sync, web-player conversion, SRT-to-ASS, VTT-to-ASS guides | Added CTR-focused SERP titles/descriptions, production SEO assertions, and current GSC queue coverage | Capture additional long-tail searches that were in the sitemap but missing from the current inspection batch and had weaker SERP snippets. | `pnpm verify:seo:ready` passed; `pnpm gsc:day0:list -- --batch current` prints 66 URLs; `pnpm verify:gsc:day0` passed for 87 URLs plus sitemap index |
| 2026-06-01 | GSC current queue plus tool conversion, sync, transcript, merge, and guide hub pages | Added current inspection coverage and production SEO assertions | Cover remaining non-policy sitemap URLs and core tool pages that can support conversion and ad-readiness paths. | `pnpm gsc:day0:list -- --batch current` prints 85 URLs; `pnpm verify:gsc:day0` passed for 106 URLs plus sitemap index; `pnpm verify:seo:ready` passed |
| 2026-06-02 | VLC subtitle failure guide, homepage, guides index, llms, GSC current queue | Added a VLC-specific troubleshooting page, internal discovery links, FAQ/schema assertions, and Asia/Shanghai default dates for growth evidence scripts | Capture VLC subtitle not showing searches and keep GSC/promotion evidence dates aligned with the user's operating timezone. | `pnpm audit:content-meta` passed; `pnpm build` built 111 pages; `pnpm audit:serp` passed for 108 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 108 URLs plus sitemap index |
| 2026-06-02 | VLC subtitle delay guide, homepage, guides index, llms, GSC current queue | Added a VLC-specific sync guide and cross-linked it from the VLC subtitle failure page | Capture VLC subtitle delay and VLC subtitles out-of-sync searches with a direct path to the subtitle delay fixer. | `pnpm audit:content-meta` passed; `pnpm build` built 112 pages; `pnpm audit:serp` passed for 109 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 109 URLs plus sitemap index |
| 2026-06-02 | Long subtitles guide, homepage, guides index, llms, GSC current queue | Added a readability-focused guide for long caption lines, cue duration, and reading speed, with discovery links and SEO assertions | Capture subtitle line length, subtitles too long, and caption readability searches with a direct path to subtitle cleanup tools. | `pnpm audit:content-meta` passed; `pnpm build` built 113 pages; `pnpm audit:serp` passed for 110 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 110 URLs plus sitemap index |
| 2026-06-02 | SRT not working guide, homepage, guides index, llms, GSC current queue | Added a diagnostic SRT playback troubleshooting guide with FAQ/schema coverage and direct paths to validator, timestamp, encoding, and conversion tools | Capture SRT file not working and subtitle playback troubleshooting searches that sit between upload failures, validation, and player-specific caption issues. | `pnpm audit:content-meta` passed; `pnpm build` built 114 pages; `pnpm audit:serp` passed for 111 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 111 URLs plus sitemap index |
| 2026-06-02 | Missing subtitles after conversion guide, homepage, guides index, llms, GSC current queue | Added a cue-loss troubleshooting guide with FAQ/schema coverage and direct paths to validators, timestamp repair, cleanup, and ASS conversion tools | Capture subtitles missing after conversion and cue loss searches that can turn into validation, repair, or reconversion tool actions. | `pnpm audit:content-meta` passed; `pnpm build` built 115 pages; `pnpm audit:serp` passed for 112 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 112 URLs plus sitemap index |
| 2026-06-02 | Empty converted subtitle guide, homepage, guides index, llms, GSC current queue | Added a blank-output troubleshooting guide with FAQ/schema coverage and direct paths to validators, timestamp repair, cleanup, and ASS conversion tools | Capture converted subtitle file empty, blank subtitle output, and no text after conversion searches that can turn into validation, repair, or reconversion tool actions. | `pnpm audit:content-meta` passed; `pnpm build` built 116 pages; `pnpm audit:serp` passed for 113 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 113 URLs plus sitemap index |
| 2026-06-02 | Subtitle drift guide, homepage, guides index, llms, GSC current queue | Added a frame-rate and progressive sync drift troubleshooting guide with FAQ/schema coverage and direct paths to sync, delay, time-shift, and partial-shift tools | Capture subtitle drift, subtitles progressively out of sync, and frame rate mismatch searches that can turn into sync repair tool actions. | `pnpm audit:content-meta` passed; `pnpm build` built 117 pages; `pnpm audit:serp` passed for 114 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 114 URLs plus sitemap index |
| 2026-06-02 | MP4 subtitle extraction guide, guides index, video extraction guide, llms, GSC current queue | Added an MP4-specific extraction guide with FAQ/schema coverage and direct paths to the browser-local video subtitle extractor | Capture extract subtitles from MP4 searches that are more specific than the general video extraction guide and can turn into extraction tool actions. | `pnpm audit:content-meta` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | VTT to SRT guide and VTT to SRT tool page | Strengthened the primary-queue guide title, meta description, FAQ/schema coverage, SRT validator follow-up, and tool-to-guide internal link | Improve CTR and ranking support for VTT to SRT searches that can turn directly into converter tool usage. | `pnpm audit:content-meta` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | Clean SRT guide | Strengthened the clean SRT guide title, meta description, FAQ/schema coverage, and validator/timestamp repair follow-up links | Improve CTR and ranking support for clean SRT file searches that can turn directly into SRT cleanup, validation, or timestamp repair tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | MKV subtitle extraction guide | Strengthened the MKV extraction guide title, meta description, FAQ/schema coverage, and no-subtitle/multiple-track troubleshooting sections | Improve CTR and ranking support for extract subtitles from MKV searches that can turn directly into browser-local video subtitle extraction. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | Video subtitle extraction guide | Strengthened the general extraction guide title, meta description, FAQ/schema coverage, and no-subtitle troubleshooting section | Improve CTR and ranking support for broad extract subtitles from video searches before users branch into MP4, MKV, or OCR-specific workflows. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | YouTube subtitle converter tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, and FAQ wording around upload-safe SRT conversion | Improve CTR and conversion support for YouTube subtitle converter searches and guide-to-tool traffic from upload failure and caption format pages. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | Subtitle cleaner tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, FAQ wording, and related cleanup/validator links around removing tags and fixing spacing | Improve CTR and conversion support for subtitle cleaner searches that can turn into cleanup, validation, or clean-SRT tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | SRT validator tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, FAQ wording, and timestamp repair follow-up around upload errors and validation reports | Improve CTR and conversion support for invalid SRT, SRT upload error, and subtitle checker searches that can turn into validation, cleanup, or timestamp repair tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | WebVTT validator tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, FAQ wording, and VTT loading follow-up around missing headers and HTML5 caption issues | Improve CTR and conversion support for WebVTT validator, VTT captions not showing, and missing WEBVTT header searches that can turn into validation, conversion, or player troubleshooting usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | SRT timestamp fixer tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, and FAQ wording around malformed SRT timestamps, dot separators, upload errors, and cue-number repair | Improve CTR and conversion support for fix SRT timestamps, malformed SRT timestamp, and dot timestamp searches that can turn into timestamp repair or SRT validation usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | SRT to VTT tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, and FAQ wording around HTML5 video, WEBVTT headers, timestamp dots, and browser-ready captions | Improve CTR and conversion support for SRT to VTT, SRT to VTT for HTML5 video, and WEBVTT header searches that can turn directly into converter usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | VTT to SRT tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, and FAQ wording around legacy subtitle editors, WEBVTT headers, timestamp commas, and editor-ready SubRip output | Improve CTR and conversion support for VTT to SRT, WebVTT to SRT, and legacy subtitle editor searches that can turn directly into converter usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | ASS to SRT tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, and FAQ wording around YouTube uploads, ASS/SSA flattening, unsupported styling, and SubRip delivery copies | Improve CTR and conversion support for ASS to SRT, SSA to SRT, and YouTube caption upload searches that can turn directly into converter usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | SRT to ASS tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, and FAQ wording around Aegisub, subtitle styling, karaoke timing, default ASS styles, and local conversion | Improve CTR and conversion support for SRT to ASS, SRT to ASS for Aegisub, and subtitle styling searches that can turn directly into converter usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | Subtitle delay fixer tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, and FAQ wording around out-of-sync subtitles, millisecond shift values, and moving captions earlier or later | Improve CTR and conversion support for subtitle delay fixer, out-of-sync subtitles, shift subtitles, and subtitles ahead or behind audio searches that can turn directly into timing repair usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | Video subtitle extractor tool page | Strengthened the primary-queue tool page title, meta description, workflow copy, and FAQ wording around extracting embedded text subtitle tracks from MKV, MP4, MOV, and WebM files without upload | Improve CTR and conversion support for extract subtitles from video, extract subtitles from MKV, extract subtitles from MP4, and embedded vs burned-in subtitle searches that can turn directly into extraction usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 118 pages; `pnpm audit:serp` passed for 115 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 115 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | MOV subtitle extraction guide | Added a MOV-specific extraction guide with FAQ/schema coverage and direct paths to the browser-local video subtitle extractor | Capture extract subtitles from MOV and QuickTime caption extraction searches that can turn directly into browser-local extraction usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 119 pages; `pnpm audit:serp` passed for 116 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 116 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | WebM subtitle extraction guide | Added a WebM-specific extraction guide with FAQ/schema coverage and direct paths to the browser-local video subtitle extractor and WebVTT validator | Capture extract subtitles from WebM and WebM caption track searches that can turn into extraction, VTT validation, or VTT-to-SRT conversion usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 120 pages; `pnpm audit:serp` passed for 116 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 117 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed |
| 2026-06-02 | AVI subtitle extraction guide | Added an AVI-specific extraction guide with sidecar subtitle, burned-in caption, and DVD-rip troubleshooting coverage | Capture extract subtitles from AVI searches that can turn into browser-local extraction, SRT validation, cleanup, or OCR triage. | `node --check scripts/verify-production-seo.mjs` passed; `git diff --check` passed; `pnpm audit:content-meta` passed; `pnpm build` built 122 pages; `pnpm audit:serp` passed; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed; `pnpm test:gsc:day0:list` passed; production gate passed for live `4065e21` and was recorded in `5d9525b` |
| 2026-06-02 | VOB subtitle extraction guide | Added a VOB/DVD-specific extraction guide with VobSub, IDX/SUB sidecar, image-subtitle, and OCR boundary coverage | Capture extract subtitles from VOB and DVD subtitle extraction searches that can turn into local extraction checks, OCR triage, and subtitle validation usage. | `node --check scripts/verify-production-seo.mjs` passed; `git diff --check` passed; `pnpm audit:content-meta` passed; `pnpm build` built 123 pages; `pnpm audit:serp` passed for 119 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 120 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed; production gate passed for live `362beb7` and was recorded in `a1e9b09` |
| 2026-06-02 | WMV subtitle extraction guide | Added a WMV/Windows Media-specific extraction guide with SMI/SAMI sidecar, player-loaded caption, and burned-in caption troubleshooting coverage | Capture extract subtitles from WMV and Windows Media caption searches that can turn into local extraction checks, sidecar caption conversion, or OCR triage. | `node --check scripts/verify-production-seo.mjs` passed; `git diff --check` passed; `pnpm audit:content-meta` passed; `pnpm build` built 124 pages; `pnpm audit:serp` passed for 120 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 121 URLs plus sitemap index; `pnpm test:gsc:day0:list` passed; production gate passed for live `682bed8` and was recorded in `26df782` |
| 2026-06-02 | Merge subtitle files guide | Added a general merge subtitle files guide, homepage/guides/llms discovery, subtitle-merger tool back-link, SEO assertions, and current GSC queue coverage | Capture merge subtitles and combine subtitle files searches that can turn directly into Subtitle Merger tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 180 pages; `pnpm audit:serp` passed for 176 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 177 URLs plus sitemap index |
| 2026-06-02 | MKV to SRT guide | Added an `extract SRT from MKV` guide, linked it from MKV extraction/failure pages, extractor tool related guides, homepage/guides/llms discovery, and the current GSC queue | Capture extract SRT from MKV, MKV to SRT, and MKV subtitles to SRT searches that can turn directly into browser-local subtitle extraction and follow-up SRT workflow usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Out-of-sync subtitles guide refresh | Expanded the `fix out of sync subtitles` guide with constant-offset vs drift vs scene-cut diagnosis, FAQ/schema coverage, clearer timing examples, and stronger links into sync repair workflows | Improve CTR and ranking support for fix out-of-sync subtitles, subtitles ahead of audio, subtitles behind speech, and timing repair searches that can turn directly into sync tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | VTT captions not loading guide refresh | Expanded the `why VTT captions are not loading` guide with file-vs-delivery triage, FAQ/schema coverage, MIME/CORS checks, concrete track examples, and stronger validator/converter/internal follow-up links | Improve CTR and ranking support for VTT captions not loading, WebVTT not loading, and browser caption troubleshooting searches that can turn directly into validator and converter tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | HTML5 subtitles not showing guide refresh | Expanded the `why subtitles do not show in HTML5 video` guide with file-vs-delivery-vs-player triage, FAQ/schema coverage, track element checks, MIME/CORS follow-ups, and stronger validator/converter links | Improve CTR and ranking support for HTML5 subtitles not showing and browser caption troubleshooting searches that can turn directly into validator and converter tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | YouTube subtitle conversion guide refresh | Expanded the `convert subtitles for YouTube` guide with source-format decision paths, YouTube-ready SRT checks, upload-failure triage, and stronger links into upload settings, preparation, and encoding follow-ups | Improve CTR and ranking support for convert subtitles for YouTube, VTT to SRT for YouTube, and upload-ready subtitle conversion searches that can turn directly into converter and validator tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | YouTube SRT vs ASS guide refresh | Expanded the `SRT vs ASS for YouTube captions` guide with upload-vs-editing decision rules, ASS-to-SRT preservation boundaries, YouTube upload checklist, and stronger links into validator, upload settings, conversion, and failure follow-ups | Improve CTR and ranking support for SRT vs ASS for YouTube captions, ASS YouTube upload, and subtitle format decision searches that can turn directly into ASS-to-SRT and YouTube conversion tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | YouTube upload failure guide refresh | Expanded the `why YouTube subtitle upload failed` guide with retry decision rules, upload rejection boundaries, a pre-upload checklist, and stronger links into SRT settings, upload preparation, SRT vs ASS, validator, and conversion follow-ups | Improve CTR and ranking support for YouTube subtitle upload failed, YouTube captions rejected, and YouTube SRT upload error searches that can turn directly into converter, validator, timestamp repair, and cleanup tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | SRT upload failure guide refresh | Expanded the `why your SRT file will not upload` guide with format-first decision rules, upload rejection boundaries, a re-upload checklist, and stronger links into cleanup, YouTube SRT settings, timestamp repair, validator, and YouTube-specific failure follow-ups | Improve CTR and ranking support for SRT file won't upload, SRT upload error, and rejected SRT caption searches that can turn directly into validator, timestamp repair, cleanup, and conversion tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | ASS to SRT YouTube guide refresh | Expanded the `convert ASS to SRT for YouTube uploads` guide with ASS/SSA-to-YouTube-ready SRT decision paths, upload-copy validation, FAQ/schema coverage, and stronger converter/validator/upload follow-up links | Improve CTR and ranking support for ASS to SRT for YouTube, SSA to YouTube SRT, and ASS subtitle upload searches that can turn directly into conversion and validation tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | SRT validation guide refresh | Added a validation-report decision table and stronger follow-up links into SRT upload failure, YouTube SRT settings, cleanup, and timestamp repair workflows | Improve CTR and ranking support for SRT validator and validate SRT before upload searches that can turn directly into validation, repair, and upload troubleshooting tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Malformed SRT timestamp guide refresh | Added CTR-focused SERP metadata, timestamp-error decision table, repair-vs-sync boundaries, upload-safe checklist, FAQ/schema coverage, and stronger validator/upload follow-up links | Improve CTR and ranking support for malformed SRT timestamp, fix SRT timestamps, dot timestamp, and SRT upload repair searches that can turn directly into timestamp repair and validation tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Embedded vs burned-in subtitles guide refresh | Added CTR-focused SERP metadata, extract-vs-OCR decision table, FAQ/schema coverage, and stronger extractor/validator/cleaner follow-up links | Improve CTR and ranking support for embedded subtitles, burned-in subtitles, and subtitle extraction searches that can turn directly into local extraction, validation, cleanup, or OCR triage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Remove SRT line numbers tool and guide refresh | Added CTR-focused SERP metadata, rebuild-vs-remove decision paths, FAQ/schema coverage, and stronger TXT/VTT/cleaner/validator follow-up links | Improve CTR and conversion support for remove SRT line numbers, rebuild SRT cue numbers, duplicate SRT numbering, and SRT renumbering searches that can turn directly into local renumbering, transcript, conversion, cleanup, or validation usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `pnpm audit:serp` passed for 178 pages; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | UTF-8 subtitle conversion guide refresh | Added CTR-focused SERP metadata, source-encoding decision paths, FAQ/schema coverage, and stronger encoding fixer/validator/cleaner follow-up links | Improve CTR and conversion support for convert subtitles to UTF-8, fix subtitle encoding, garbled subtitle text, boxes, and broken accent searches that can turn directly into local encoding repair usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Transcript generator guide and tool refresh | Added source-format decision paths, FAQ/schema coverage, transcript-vs-caption boundaries, and stronger TXT/encoding follow-up links | Improve CTR and conversion support for create transcript from subtitles, subtitle transcript generator, subtitles to text, remove timestamps, and SRT/VTT/ASS transcript searches that can turn directly into local transcript generation usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | YouTube subtitle format guide refresh | Added CTR-focused SERP metadata, a source-format decision table, FAQ/schema coverage, and stronger converter/validator follow-up links | Improve CTR and conversion support for best subtitle format for YouTube, YouTube subtitle format, SRT upload, VTT to YouTube, and ASS to YouTube searches that can turn directly into local conversion or validation usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | SRT to VTT HTML5 guide refresh | Added no-upload SERP metadata, a conversion/validation/delivery decision table, FAQ/schema coverage, and stronger SRT validator, WebVTT validator, VTT-to-SRT, and TXT follow-up links | Improve CTR and conversion support for convert SRT to VTT, SRT to VTT for HTML5 video, WEBVTT header, and browser caption searches that can turn directly into local conversion or validation usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Clean SRT tool page refresh | Strengthened the `/tools/clean-srt-file/` title, meta description, workflow copy, FAQ coverage, and validator/timestamp repair follow-up links around upload-ready SRT cleanup | Improve CTR and conversion support for clean SRT file, SRT cleaner, fix SRT spacing, and rebuild SRT cue number searches that can turn directly into local cleanup and validation usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Subtitle time shifter tool page refresh | Strengthened the `/tools/subtitle-time-shifter/` title, meta description, workflow copy, FAQ coverage, and delay/partial-shift follow-up links around millisecond subtitle timing offsets | Improve CTR and conversion support for shift subtitle timing, subtitle time shifter, move subtitles earlier or later, and SRT/VTT/ASS timing offset searches that can turn directly into local sync repair usage. | `node --check scripts/verify-production-seo.mjs` passed; `node --check src/lib/subtitles/catalog.ts` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Fix out-of-sync subtitles tool page refresh | Strengthened the `/tools/fix-out-of-sync-subtitles/` title, meta description, workflow copy, FAQ coverage, and time-shifter/partial-shifter/validator follow-up links around whole-file subtitle sync offsets | Improve CTR and conversion support for fix out-of-sync subtitles, subtitles ahead of audio, subtitles behind speech, and SRT/VTT/ASS sync repair searches that can turn directly into local timing repair usage. | `node --check scripts/verify-production-seo.mjs` passed; `node --check src/lib/subtitles/catalog.ts` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Scene-cut and partial subtitle shift refresh | Strengthened the scene-cut sync guide, partial-range shift guide, and `/tools/partial-subtitle-shifter/` SERP/workflow copy with FAQ coverage and full-file/drift fallback links | Improve CTR and conversion support for shift one subtitle section, fix subtitles after a scene cut, and selected-range SRT/VTT/ASS timing searches that can turn directly into local partial-shift usage. | `node --check scripts/verify-production-seo.mjs` passed; `node --check src/lib/subtitles/catalog.ts` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Out-of-order subtitle cues guide refresh | Added CTR-focused SERP metadata, FAQ/schema coverage, sort-vs-shift decision guidance, and stronger SRT validator, Clean SRT, cleaner, and overlap follow-up links | Improve CTR and ranking support for out-of-order subtitle cues, sort SRT cues, cue order repair, and subtitle timing block searches that can turn into validation and cleanup tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Remove HTML tags from subtitles guide refresh | Added no-upload SERP metadata, FAQ/schema coverage, clean-vs-convert decision guidance, before/after examples, guides index discovery, and stronger cleaner/SRT/WebVTT validator links | Improve CTR and conversion support for remove HTML tags from subtitles, clean subtitle tags, strip font/span tags, and upload-safe caption cleanup searches that can turn directly into local cleaner and validator usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Subtitle line breaks guide refresh | Added no-upload SERP metadata, FAQ/schema coverage, practical line-length targets, clean-vs-transcript decision guidance, before/after wrap examples, and stronger cleaner/SRT validator/long-subtitle follow-up links | Improve CTR and conversion support for fix subtitle line breaks, clean SRT line breaks, awkward caption wraps, and readable subtitle formatting searches that can turn directly into local cleaner and validator usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Overlapping subtitles guide refresh | Added no-upload SERP metadata, FAQ/schema coverage, overlap examples, shorten-vs-shift-vs-sort decision guidance, and stronger cleaner/SRT validator/partial-shift/fix-sync links | Improve CTR and conversion support for overlapping subtitles, fix overlapping SRT cues, subtitle timing conflicts, and cue overlap repair searches that can turn directly into local cleaner, validator, and sync repair tool usage. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Export sync guide refresh | Added no-upload SERP metadata, FAQ/schema coverage, export-trim/scene-cut/drift decision guidance, offset measurement examples, and stronger sync/delay/partial/time-shifter links | Improve CTR and conversion support for subtitles out of sync after export, subtitle timing after export, trimmed video subtitle delay, and scene-cut export searches that can turn directly into local sync repair tools. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
| 2026-06-02 | Fast or slow subtitles guide refresh | Added no-upload SERP metadata, FAQ/schema coverage, early-vs-late-vs-drift decision guidance, sign-safe offset examples, and stronger time-shifter/delay/partial/fix-sync links | Improve CTR and conversion support for subtitles too fast, subtitles too slow, captions ahead of audio, captions behind speech, and drift-vs-delay searches that can turn directly into local timing repair tools. | `node --check scripts/verify-production-seo.mjs` passed; `pnpm audit:content-meta` passed; `git diff --check` passed; `pnpm build` built 181 pages; `pnpm audit:serp` passed for 178 pages; `pnpm verify:seo:dist` passed; `pnpm verify:gsc:day0` passed for 178 URLs plus sitemap index |
